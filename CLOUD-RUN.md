# Browser-only development

GitHub Codespaces is the supported browser-only development environment. It provides PHP 8.3+, Composer and SQLite without installing tools on Windows.

Codespaces is not permanent hosting. For a persistent environment, deploy the Docker Compose production stack to a Linux host or adapt it to a managed platform with PostgreSQL and Redis.
