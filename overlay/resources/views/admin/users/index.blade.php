@extends('layouts.app')
@section('title','Players')
@section('content')<div class="page-head"><div><span class="eyebrow">ADMIN</span><h1>Players</h1></div><a class="button secondary" href="{{ route('admin.dashboard') }}">Overview</a></div><section class="panel"><form method="get" class="search-row"><input type="search" name="q" value="{{ request('q') }}" placeholder="Search name or email"><button class="button" type="submit">Search</button></form><div class="table-wrap"><table><thead><tr><th>Player</th><th>Balance</th><th>Rounds</th><th>Status</th><th></th></tr></thead><tbody>@foreach($users as $user)<tr><td><strong>{{ $user->name }}</strong><br><small>{{ $user->email }}</small></td><td>{{ number_format($user->wallet?->balance ?? 0) }}</td><td>{{ $user->game_entries_count }}</td><td>{{ $user->isSuspended()?'Suspended':'Active' }}</td><td><a href="{{ route('admin.users.show',$user) }}">Manage →</a></td></tr>@endforeach</tbody></table></div>{{ $users->links() }}</section>@endsection
